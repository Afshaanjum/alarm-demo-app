apiVersion: v1
kind: Secret
metadata:
  name: alarm-demo-secret
type: Opaque
stringData:
# NEVER store sensitive values plainly in Git in production — use sealed-secrets or SOPS
  ALARM_API_KEY: "super-secret-api-key"